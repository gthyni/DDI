apiVersion: v1
kind: Secret
metadata:
  name: dns-db-secret
type: Opaque
stringData:
  # In a real production environment, use SealedSecrets or Vault
  POSTGRES_USER: "dnsadmin"
  POSTGRES_PASSWORD: "dnspassword"
  POSTGRES_DB: "dnsmaster"
