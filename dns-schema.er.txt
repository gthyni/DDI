erDiagram
    DOMAINS ||--o{ RECORDS : contains
    DOMAINS {
        bigint id PK
        varchar name "Unikt zon-namn"
        varchar type "MASTER/SLAVE/NATIVE"
        int soa_serial "Versionshantering"
        timestamp created_at
        varchar comments
    }
    RECORDS {
        bigint id PK
        bigint domain_id FK "Länk till DOMAINS"
        varchar name "FQDN (t.ex. www.example.com)"
        varchar type "Record type (A, MX, TXT)"
        varchar content "Datat (IP, Text, etc)"
        int ttl "Time To Live"
        int prio "Prioritet (för MX)"
        boolean disabled "for swiftly (temporarly) disable a record"
        int subnetid "VLAN ID for IPPlan"
        varchar comments
    }
    SUBNETS { 
        int subnetid
        varchar ipaddress 
        int cidr
        int vrf
        int VLAN
        varchar location
        varchar comments
    }



